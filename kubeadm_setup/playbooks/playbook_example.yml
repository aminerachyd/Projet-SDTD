- become: yes
  hosts: all
  tasks:
    - name: Ensure nginx is at the latest version
      apt:
        name: nginx
        state: latest
    - name: Start nginx
      service:
        name: nginx
        state: started
